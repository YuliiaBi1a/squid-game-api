spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/squid_game_db
    username: yulia
    password: yulia
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
